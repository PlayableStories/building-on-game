{
  /**
   * A solution file. The real configuration is split in two so that the code
   * that ships to a browser cannot see the tooling that tests it:
   *
   *   tsconfig.app.json   src/, minus tests. No Node types, no test globals.
   *   tsconfig.node.json  configs, scripts, e2e and the tests themselves.
   *
   * `npm run build` runs `tsc -b`, which checks both.
   */
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
